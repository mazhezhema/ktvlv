# 代码目录结构 = 模块责任结构（定版）

> **文档版本**：v1.0  
> **最后更新**：2025-12-30  
> **状态**：✅ 定版（不可再争）  
> **用途**：目录即责任，接口即合同

---

## 📁 完整目录结构

```text
ktvlv/
├─ src/
│  ├─ main.cpp                    # 启动流程（PM A，工程师B代写）
│  │
│  ├─ events/                      # 事件系统（PM A，框架层）
│  │  ├─ event_types.h            # Event ID / Event 结构定义（PM A）
│  │  ├─ event_queue.h/.cpp       # 事件队列
│  │  └─ event_dispatcher.h/.cpp  # 事件分发器
│  │
│  ├─ app/                         # 应用层（PM A）
│  │  └─ AppController.h/.cpp     # 应用控制器（编排，PM定模板）
│  │
│  ├─ ui/                          # UI 层（工程师A）
│  │  ├─ page_manager.h/.cpp      # 页面管理器
│  │  ├─ pages/                    # 页面实现
│  │  │  ├─ PageHome.h/.cpp
│  │  │  ├─ PageSearch.h/.cpp
│  │  │  └─ PageCategory.h/.cpp
│  │  ├─ components/               # UI 组件
│  │  │  ├─ SongListItem.h/.cpp
│  │  │  └─ SearchBar.h/.cpp
│  │  └─ ui_state.h/.cpp          # UI 状态管理（loading/empty/error）
│  │
│  ├─ features/                    # 业务层（工程师B）
│  │  ├─ category/
│  │  │  ├─ CategoryController.h/.cpp   # Controller（编排）
│  │  │  ├─ CategoryService.h/.cpp      # Service（业务逻辑）
│  │  │  └─ CategoryModel.h/.cpp        # Model（数据模型）
│  │  ├─ song/
│  │  │  ├─ SongController.h/.cpp
│  │  │  ├─ SongService.h/.cpp
│  │  │  └─ SongModel.h/.cpp
│  │  └─ rank/
│  │     ├─ RankController.h/.cpp
│  │     ├─ RankService.h/.cpp
│  │     └─ RankModel.h/.cpp
│  │
│  ├─ services/                    # Service 层（工程师B）
│  │  ├─ network_service.h/.cpp   # NetworkService（libcurl封装，PM A接口）
│  │  ├─ player_service.h/.cpp    # PlayerService
│  │  ├─ history_service.h/.cpp   # HistoryService
│  │  └─ cache_service.h/.cpp     # CacheService
│  │
│  ├─ models/                      # 数据模型（工程师B）
│  │  ├─ Song.h/.cpp
│  │  ├─ Category.h/.cpp
│  │  └─ Rank.h/.cpp
│  │
│  ├─ adapter/                     # Adapter 层（高风险，PM A接口，工程师B实现）
│  │  ├─ network_adapter.h/.cpp   # NetworkAdapter（PM A接口）
│  │  ├─ player_adapter.h/.cpp    # PlayerAdapter（PM A接口）
│  │  └─ storage_adapter.h/.cpp   # StorageAdapter（PM A接口）
│  │
│  └─ platform/                    # 平台层（系统依赖）
│     └─ f133_linux/
│        ├─ display_fbdev.c
│        ├─ input_evdev.c
│        └─ audio_alsa.c
│
├─ docs/                           # 文档目录
└─ CMakeLists.txt                  # 构建配置
```

---

## 📋 目录责任说明

### 1️⃣ `src/events/` - 事件系统（PM A，框架层）

**责任人**：PM（A - 决策），工程师（I - 知情）

**职责**：
- `event_types.h`：Event ID / Event 结构定义（**PM A**）
- `event_queue.h/.cpp`：事件队列实现（框架层）
- `event_dispatcher.h/.cpp`：事件分发器实现（框架层）

**禁止项**：
- ❌ 工程师不能修改 `event_types.h`
- ❌ 工程师不能修改 Event 结构

---

### 2️⃣ `src/app/` - 应用层（PM A）

**责任人**：PM（A - 决策），工程师B（R - 实现）

**职责**：
- `AppController.h/.cpp`：应用控制器，编排逻辑（PM定模板）

---

### 3️⃣ `src/ui/` - UI 层（工程师A）

**责任人**：工程师A（R - 实现），PM（I - 知情，UI状态策略 A）

**职责**：
- `pages/`：页面实现（LVGL）
- `components/`：UI 组件
- `ui_state.h/.cpp`：UI 状态管理（loading/empty/error，PM A）

**禁止项**：
- ❌ 不能 include `services/*`
- ❌ 不能 include `features/*/Service`
- ❌ 不能直接调用 Service

**允许项**：
- ✅ 可以 include `events/event_queue.h`（发送 Event）
- ✅ 可以 include `features/*/Controller.h`（调用 Controller）

---

### 4️⃣ `src/features/` - 业务层（工程师B）

**责任人**：工程师B（R - 实现），PM（I - 知情）

**职责**：
- `{Module}Controller.h/.cpp`：Controller（编排逻辑）
- `{Module}Service.h/.cpp`：Service（业务逻辑）
- `{Module}Model.h/.cpp`：Model（数据模型）

**禁止项**：
- ❌ Controller 不能解析 JSON
- ❌ Controller 不能做业务逻辑
- ❌ Service 不能操作 UI

**允许项**：
- ✅ Controller 可以调用 Service
- ✅ Service 可以调用 NetworkService
- ✅ Service 可以发送 Event

---

### 5️⃣ `src/services/` - Service 层（工程师B）

**责任人**：工程师B（R - 实现），PM（NetworkService接口 A）

**职责**：
- `network_service.h/.cpp`：NetworkService（libcurl封装，**PM A接口**）
- `player_service.h/.cpp`：PlayerService
- `history_service.h/.cpp`：HistoryService
- `cache_service.h/.cpp`：CacheService

**禁止项**：
- ❌ 不能 include `ui/*`
- ❌ 不能操作 LVGL 控件
- ❌ NetworkService 接口不能修改（PM A）

---

### 6️⃣ `src/models/` - 数据模型（工程师B）

**责任人**：工程师B（R - 实现），PM（I - 知情）

**职责**：
- 数据模型定义（Song、Category、Rank等）
- JSON 序列化/反序列化

---

### 7️⃣ `src/adapter/` - Adapter 层（高风险，PM A接口）

**责任人**：PM（A - 接口定义），工程师B（R - 实现，按接口）

**职责**：
- `network_adapter.h/.cpp`：NetworkAdapter（**PM A接口**，工程师B实现）
- `player_adapter.h/.cpp`：PlayerAdapter（**PM A接口**，工程师B实现）
- `storage_adapter.h/.cpp`：StorageAdapter（**PM A接口**，工程师B实现）

**禁止项**：
- ❌ 工程师不能修改 Adapter 接口（`.h` 文件）
- ❌ 接口变更必须 PM review

---

### 8️⃣ `src/platform/` - 平台层（系统依赖）

**责任人**：框架层维护

**职责**：
- 平台特定实现（F133 Linux）
- 驱动抽象

---

## 🔍 检查法（每日必查）

### 依赖检查

```bash
# UI 层检查：不能 include service
grep -r "#include.*service" src/ui/
# 应该返回：无结果或只有 events/event_queue.h

# Service 层检查：不能 include ui
grep -r "#include.*ui" src/services/ src/features/
# 应该返回：无结果

# Adapter 接口检查：接口是否被修改
git diff src/adapter/*.h
# 如果有变更，必须 PM review
```

---

### 文件位置检查

| 文件类型 | 应该在哪里 | 禁止在哪里 |
|---------|----------|----------|
| **UI 页面** | `src/ui/pages/` | 不在 `src/services/`、`src/features/` |
| **Service** | `src/features/*/Service.h` 或 `src/services/` | 不在 `src/ui/` |
| **Controller** | `src/features/*/Controller.h` | 不在 `src/ui/`、`src/services/` |
| **Adapter** | `src/adapter/` | 不在其他目录 |
| **Event 定义** | `src/events/event_types.h` | 不在其他目录 |

---

### 接口检查

| 接口类型 | 谁定义 | 谁实现 | 谁review |
|---------|--------|--------|----------|
| **Event 结构** | PM（A） | 框架层 | PM |
| **NetworkService 接口** | PM（A） | 工程师B | PM |
| **Adapter 接口** | PM（A） | 工程师B | PM |
| **Controller 接口** | PM（A，模板） | 工程师B | PM |
| **UI 状态接口** | PM（A） | 工程师A | PM |

---

## 📊 目录与责任映射表

| 目录 | PM | 工程师A | 工程师B | 说明 |
|------|-----|---------|---------|------|
| `events/event_types.h` | **A** | I | I | Event 定义 |
| `ui/` | I（UI状态A） | **R** | I | UI 层 |
| `features/` | I | I | **R** | 业务层 |
| `services/network_service.h` | **A** | I | **R** | NetworkService 接口 |
| `adapter/*.h` | **A** | I | R（按接口） | Adapter 接口 |
| `adapter/*.cpp` | C（review） | I | **R** | Adapter 实现 |
| `app/` | **A** | I | R | 应用层 |
| `main.cpp` | **A** | I | R | 启动流程 |

---

## 🚫 禁止的跨目录依赖

### ❌ UI 层禁止

```cpp
// ❌ 错误：UI 层 include Service
#include "services/network_service.h"  // 禁止！
#include "features/category/CategoryService.h"  // 禁止！

// ✅ 正确：UI 层只 include Event 和 Controller
#include "events/event_queue.h"
#include "features/category/CategoryController.h"
```

---

### ❌ Service 层禁止

```cpp
// ❌ 错误：Service 层 include UI
#include "ui/pages/PageCategory.h"  // 禁止！
#include "ui/ui_state.h"  // 禁止！

// ✅ 正确：Service 层只 include Service 和 Event
#include "services/network_service.h"
#include "events/event_queue.h"
```

---

### ❌ Controller 禁止

```cpp
// ❌ 错误：Controller 解析 JSON
#include "cJSON.h"
void CategoryController::handleData(const char* json) {
    cJSON* root = cJSON_Parse(json);  // 禁止！应该在 Service 层
}

// ✅ 正确：Controller 只编排
void CategoryController::onUiCategoryClicked(int categoryId) {
    CategoryService::instance().load(categoryId);
}
```

---

## ✅ 允许的依赖方向

```
UI层 (src/ui/)
  ↓ include
Controller (src/features/*/Controller.h)
  ↓ include
Service (src/features/*/Service.h)
  ↓ include
NetworkService (src/services/network_service.h)
  ↓ include
Adapter (src/adapter/network_adapter.h)
  ↓ include
SDK (libcurl、sqlite、tplayer)
```

**依赖方向**：UI → Controller → Service → NetworkService → Adapter → SDK

**禁止反向依赖**：SDK → Adapter → Service → Controller → UI

---

## 🔧 文件命名规范

### 类名和文件名

```cpp
// 类名：PascalCase
class SearchController { };
class SongModel { };
class NetworkService { };

// 文件名：与类名一致，使用下划线分隔单词（可选）
SearchController.h / SearchController.cpp
SongModel.h / SongModel.cpp
NetworkService.h / NetworkService.cpp
```

### 目录名

- 使用小写字母
- 单词之间使用下划线分隔（可选，或直接连写）
- 例如：`charts/`、`play_list/` 或 `playlist/`

---

## 📝 代码组织示例

### 示例1：搜索功能模块

```
src/features/search/
 ├─ SearchController.h/.cpp    # 控制器
 ├─ SearchService.h/.cpp       # 业务逻辑
 └─ SearchModel.h/.cpp         # 搜索相关模型（如果有）
```

**SearchController.h**：
```cpp
#pragma once
#include <string>
#include <vector>
#include "models/Song.h"
#include "events/event_queue.h"
#include "events/event_types.h"

class SearchController {
public:
    static SearchController& instance();
    
    // ========== UI 入口（来自 View）==========
    // ✅ 正确：UI 入口使用 onUiXxx 命名
    void onUiSearchSubmitted(const std::string& keyword);
    void onUiSearchTextChanged(const std::string& text);
    
    // ========== Service 回调入口（来自 Service）==========
    // ✅ 正确：Service 回调入口使用 onSvcXxx 命名
    void onSvcSearchResult(const std::vector<Song>& songs);
    void onSvcNetworkError(int errCode, const std::string& msg);
    
private:
    SearchController() = default;
    ~SearchController() = default;
    SearchController(const SearchController&) = delete;
    SearchController& operator=(const SearchController&) = delete;
    
    // ========== 内部处理函数 ==========
    // ✅ 正确：内部函数使用 handleXxx 命名
    void handleSearchRequest(const std::string& keyword);
    void handleSearchResult(const std::vector<Song>& songs);
    void handleNetworkError(int errCode);
    
private:
    std::string m_currentKeyword;
    std::vector<Song> m_results;
};
```

**SearchService.h**：
```cpp
#pragma once
#include <string>
#include <vector>
#include "models/Song.h"

class SearchService {
public:
    static SearchService& instance();
    
    std::vector<Song> search(const std::string& keyword);
    std::vector<Song> searchBySinger(const std::string& singer);
    
private:
    // 内部实现
};
```

---

### 示例2：数据模型

```
src/models/
 └─ Song.h/.cpp
```

**Song.h**：
```cpp
#pragma once
#include <string>
#include <vector>
#include "utils/json_helper.h"  // JSON封装

class Song {
public:
    int id;
    std::string title;
    std::string singer;
    std::string url;
    std::string cover;
    int duration;  // 秒
    bool isVip;
    
    // JSON序列化
    static Song fromJson(const Json& json);
    static std::vector<Song> fromJsonList(const Json& json);
    Json toJson() const;
};
```

---

## 🚀 创建新功能模块的步骤

### 步骤1：创建目录和文件

```bash
mkdir -p src/features/new_feature
touch src/features/new_feature/NewFeatureController.h
touch src/features/new_feature/NewFeatureController.cpp
touch src/features/new_feature/NewFeatureService.h
touch src/features/new_feature/NewFeatureService.cpp
touch src/features/new_feature/NewFeatureModel.h
touch src/features/new_feature/NewFeatureModel.cpp
```

### 步骤2：实现Model（如果有）

```cpp
// NewFeatureModel.h
#pragma once
#include "utils/json_helper.h"

class NewFeatureModel {
public:
    // 字段定义
    // fromJson / toJson 方法
};
```

### 步骤3：实现Service

```cpp
// NewFeatureService.h
#pragma once
#include "services/network_service.h"
#include "NewFeatureModel.h"

class NewFeatureService {
public:
    static NewFeatureService& instance();
    
    // 业务方法
private:
    // 内部实现
};
```

### 步骤4：实现Controller

```cpp
// NewFeatureController.h
#pragma once
#include "events/event_queue.h"
#include "events/event_types.h"
#include "NewFeatureModel.h"

class NewFeatureController {
public:
    static NewFeatureController& instance();
    
    // ========== UI 入口 ==========
    void onUiFeatureClicked(int featureId);
    
    // ========== Service 回调入口 ==========
    void onSvcFeatureDataReady(int featureId, void* data);
    
private:
    NewFeatureController() = default;
    ~NewFeatureController() = default;
    NewFeatureController(const NewFeatureController&) = delete;
    NewFeatureController& operator=(const NewFeatureController&) = delete;
    
    // 内部处理
    void handleFeatureRequest(int featureId);
    void handleFeatureResult(int featureId, void* data);
};
```

### 步骤5：在EventDispatcher中注册路由

```cpp
// events/event_dispatcher.cpp
#include "features/new_feature/NewFeatureController.h"

void EventDispatcher::dispatch(const AppEvent& ev) {
    switch (ev.type) {
        case EventType::EVENT_NEW_FEATURE_CLICK:
            NewFeatureController::instance().onUiFeatureClicked(ev.arg1);
            break;
        // ...
    }
}
```

---

## 📚 相关文档

- [模块责任表（RACI最终版）.md](./模块责任表（RACI最终版）.md) ⭐⭐⭐ **必读**
- [Tina_KTV项目10条铁律（贴墙版）.md](./Tina_KTV项目10条铁律（贴墙版）.md) ⭐⭐⭐ **必读**
- [应用层命名规范（架构约束版）.md](./应用层命名规范（架构约束版）.md) ⭐⭐⭐ **必读**
- [最小模板包（可直接拷贝）.md](./最小模板包（可直接拷贝）.md) ⭐⭐⭐ **必读**

---

**最后更新**: 2025-12-30  
**状态**: ✅ 定版（目录即责任）  
**维护者**: Tech Product Owner

